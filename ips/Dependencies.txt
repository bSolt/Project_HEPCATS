IPS code runs in python and requires various libraries and dependencies

Install pip and opencv first
	sudo apt install python3-pip
	sudo apt install python3-opencv

Then pip install many libraries which will be used
	pip3 install numpy
	pip3 install pillow
	pip3 install tensorflow
	# pip3 install keras # this is no longer necesarry