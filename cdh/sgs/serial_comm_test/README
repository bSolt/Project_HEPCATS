Make sure you install socat:
	$sudo apt get socat

Make sure you install xterm:
	$sudo apt get xterm

Running the test

1. Create virtual comm ports:
	  $socat -d -d pty,raw,echo=0 pty,raw,echo=0

2. Run bash wrapper script:
	  $./serial_comm_test.sh